export interface ISliderData {
  id: number;
  title: string;
  description: string;
  buttonLink?: string;
  ImgUrl: string;
}
export interface INavLink {
  id: number;
  title: string;
  icon?: React.ReactNode;
  href: string;
}
